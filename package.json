{
  "name": "retrospective",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first Codex plugin for carrying lessons across work sessions.",
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "node scripts/clean.mjs",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}

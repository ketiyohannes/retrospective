# Retrospective

Retrospective is a local-first Codex plugin for carrying useful lessons across work sessions.

## Development

```sh
npm install
npm run typecheck
npm test
npm run build
```

Phase 0 provides the validated plugin and TypeScript scaffolding. Knowledge storage and hook behavior are added in later phases.
